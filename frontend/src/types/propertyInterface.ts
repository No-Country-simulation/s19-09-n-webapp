export interface Property {
  id?: number,
  owner?: number,
  title?: string,
  description?: string,
  address?: string,
  city?: string,
  propertyType?: "house" | "appartment",
  maxOccupants?: number,
  roomsAvailable?: number,
  price?: number,
  furnished?: boolean,
  services?: boolean,
  imgUrl?: string,
}